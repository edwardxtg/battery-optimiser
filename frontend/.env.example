# Base URL of the Cloud Run backend. Set this in Vercel (or your host) at build time.
# Locally, defaults to http://localhost:8080 if unset.
NEXT_PUBLIC_API_URL=https://battery-optimiser-xxxxx.a.run.app
